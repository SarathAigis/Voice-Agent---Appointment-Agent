# Voice Pipeline - Local Alternatives
livekit-agents>=0.8.0
livekit-plugins-silero>=0.6.0

# Local STT (replaces Deepgram)
openai-whisper>=20231117
faster-whisper>=1.0.0

# Local TTS (replaces ElevenLabs)
TTS>=0.22.0
pydub>=0.25.1

# Local LLM (replaces OpenAI)
# Use Ollama - install separately

# Core utilities
python-dotenv>=1.0.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
structlog>=24.1.0

# Optional: If you want to use OpenAI-compatible local servers
# openai>=1.12.0  # Can point to local LLM servers

# Audio processing
numpy>=1.24.0
scipy>=1.11.0
soundfile>=0.12.1

# Development
pytest>=7.4.4
pytest-asyncio>=0.23.3
black>=24.1.1
